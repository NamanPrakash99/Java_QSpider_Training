package Collections;
import java.util.Comparator;

import Comparator.Trainer;

public class SortByName implements Comparator<Trainer> {
	@Override
	public int compare(Trainer t1, Trainer t2) {
		return t1.tname.compareTo(t2.tname);
	}
}
